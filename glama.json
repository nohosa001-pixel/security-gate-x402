{
  "$schema": "https://glama.ai/mcp/schema/v1.json",
  "name": "agent-security-gate-x402",
  "title": "Agent Output Security & Hallucination Gate (x402)",
  "description": "Ultra-low latency (<10ms) deterministic security, prompt injection, secret key leak, dangerous AST execution, and factual hallucination micro-oracle for autonomous agents with EIP-191 cryptographic attestation.",
  "version": "1.0.0",
  "author": {
    "name": "Agent Security Gate Team"
  },
  "homepage": "https://github.com/your-username/agent-security-gate-x402",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/agent-security-gate-x402.git"
  },
  "license": "MIT",
  "keywords": [
    "security",
    "guardrails",
    "hallucination",
    "prompt-injection",
    "secret-leak",
    "ast-security",
    "attestation",
    "x402",
    "base",
    "mcp",
    "glama"
  ],
  "runtime": "python",
  "entrypoint": "mcp_server.py",
  "installation": {
    "pip": "pip install -r requirements.txt",
    "run": "python mcp_server.py"
  },
  "tools": [
    {
      "name": "inspect_agent_output",
      "description": "Inspects agent textual or code output for prompt injections, private key/secret leaks, dangerous AST code executions, and factual/numerical hallucinations against ground truth. Issues an EIP-191 signed Proof-of-Safety attestation.",
      "parameters": {
        "type": "object",
        "properties": {
          "agent_output": {
            "type": "string",
            "description": "The raw textual or code output generated by an autonomous agent"
          },
          "is_code": {
            "type": "boolean",
            "default": false,
            "description": "Set to true if agent_output is executable Python / shell code"
          },
          "context_ground_truth": {
            "type": "string",
            "description": "Original reference context to check for factual accuracy and numerical hallucinations"
          }
        },
        "required": ["agent_output"]
      }
    }
  ]
}
